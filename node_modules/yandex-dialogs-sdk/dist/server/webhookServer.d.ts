import { IApiRequest } from '../api/request';
import { IApiResponse } from '../api/response';
declare type HandleAliceRequestType = (request: IApiRequest) => Promise<IApiResponse>;
export interface IWebhookServerConfig {
    port: number;
    webhookUrl: string;
    options: object;
    handleRequest: HandleAliceRequestType;
}
export interface IWebhookServer {
    start(): void;
    stop(): void;
}
export declare class WebhookServer {
    private _server;
    private _port;
    private _webhookUrl;
    private _isStarted;
    private _handleAliceRequest;
    constructor(config: IWebhookServerConfig);
    private _handleRequest;
    private static _readRequest;
    private static _sendResponse;
    start(): void;
    stop(): void;
}
export {};
