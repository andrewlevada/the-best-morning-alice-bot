import { ISessionStorage, ISession } from './session';
export interface IInMemorySessionsStorageParams {
    ttl?: number;
}
export declare class InMemorySessionStorage implements ISessionStorage {
    private readonly _ttl;
    private readonly _sessions;
    constructor(params?: IInMemorySessionsStorageParams);
    private _flushOutdatedSessions;
    getOrCreate(id: string): Promise<ISession>;
}
