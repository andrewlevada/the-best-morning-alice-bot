import { Middleware } from '../middleware/middleware';
import { ISessionStorage } from './session';
import { IContext } from '../context';
import { ISessionContext } from './sessionContext';
import { ISessionKeyProvider } from './sessionKeyProvider';
interface ISessionMiddlewareParams {
    keyProvider?: ISessionKeyProvider;
}
/**
 * Uses data.session.session_id as a key
 * to store user sessions
 */
export declare function sessionMiddleware(storage: ISessionStorage, { keyProvider }?: ISessionMiddlewareParams): Middleware<ISessionContext, IContext>;
export {};
